import styled from 'styled-components'; 

const styles = {
  sectionTitle:
    "text-purplerest font-bold lg:text-[60px] md:text-[40px] sm:text-[40px] text=[50px] font-maintoo",
  sectionPar:
    "text-[#781462] md:text-[28px] sm:text-[20px] xs:text-[10px] text-[25px] text-justify font-main font-normal",
};
export { styles };

export const Button = styled.div` 
   position: fixed;  
   width: 100%; 
   left: 50%; 
   bottom: 40px; 
   height: 20px; 
   font-size: 3rem; 
   z-index: 1; 
   cursor: pointer;
   color: #7B0E90c3; 
`